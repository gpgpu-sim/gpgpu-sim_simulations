#!/bin/bash
cd _RUNDIR_REPLACE_ # STUPID -- TORQUE default runs from $HOME... 
export BENCH_BIN_DIR=_BENCHMARK_BIN_DIR_
export BENCH_NAME=_BENCHMARK_NAME_
export GPGPUSIM_DISTRIBUTION=_GPGPUSIM_DISTRIBUTION_REPLACE_

source "$GPGPUSIM_DISTRIBUTION/setup_environment"

$BENCH_BIN_DIR/$BENCH_NAME
